services:
  app-frontend:
    image: node:22
    command: sh -c "npm run dev --workspace=@lh/app -- --host 0.0.0.0 --port 5173"
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8080:5173"

    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped
    